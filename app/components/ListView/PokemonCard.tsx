import { PokemonCardPropTypes } from "@/app/_types/propTypes";

const PokemonCard = ({
	name,
	image,
	types,
	abilities,
}: PokemonCardPropTypes) => {
	return <div></div>;
};

export default PokemonCard;
